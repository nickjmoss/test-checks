name: PR

on:
  pull_request:
    types:
      - synchronize
      - opened
      - reopened

jobs:
  pr:
    runs-on: ubuntu-latest
    steps:
      - name: pass
        run: |
          echo "This PR is required. Please review and merge it."
          exit 1
