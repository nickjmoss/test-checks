name: Review

on:
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: pass
        run: |
          echo "This PR is required. Please review and merge it."
          exit 1
