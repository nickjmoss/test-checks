name: Review Change

on:
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed

jobs:
  on-review:
    name: Check Required Reviews
    permissions:
      contents: read
      pull-requests: write
      checks: write
      statuses: write
      security-events: write
      id-token: write
      actions: read
      packages: read
    secrets: inherit
    uses: nickjmoss/.github/.github/workflows/on-review.yaml@main
